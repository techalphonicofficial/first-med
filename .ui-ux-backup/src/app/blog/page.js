import { StaticPage } from "@/components/static/StaticPage";
export default function BlogPage() { return <StaticPage title="Health notes" text="Blog is intentionally minimal because wellness content is a future idea, not a current product priority." />; }
